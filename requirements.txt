# Runtime dependencies, pinned for reproducibility.
# These exact versions produced the committed metrics.json and figures.
# (Install dev/test tooling separately: pip install -r requirements-dev.txt)

# Core data & modelling
pandas==2.3.3
numpy==2.5.0
scikit-learn==1.9.0
joblib==1.5.3

# Plots for EDA / evaluation figures
matplotlib==3.11.0

# Experiment tracking (bonus)
mlflow==3.14.0

# REST API (bonus)
fastapi==0.138.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
