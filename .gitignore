# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# ML / experiment outputs (regenerated by `python -m src.train`)
mlruns/
mlartifacts/
mlflow.db
models/*.joblib
# NOTE: artifacts/ (figures + metrics.json + eda_summary.json) ARE committed as
# the canonical, reviewable results so report.md renders on GitHub without a
# local run. They are deterministic (fixed seed).

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Upstream assignment brief repo (a separate clone of neustackapp/assignment).
# The dataset and the ML brief are copied into data/ and ASSIGNMENT.md, so this
# folder is excluded from the submission repo.
assignment/
!models/.gitkeep
!artifacts/.gitkeep
